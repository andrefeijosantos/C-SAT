# Tiny CNF Dataset

This is a small collection of handcrafted CNF instances designed for quick testing of SAT solvers.

Each file is a simple Boolean formula written in standard **DIMACS CNF format**, containing only a few variables and clauses. These instances are intentionally minimal to make solver steps easy to trace and understand.
